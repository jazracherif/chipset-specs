#!/bin/sh
# Validate all chip spec YAML files against the JSON Schema before committing.
echo "Running spec validation..."
node scripts/validate-data.mjs
if [ $? -ne 0 ]; then
  echo ""
  echo "Commit aborted: fix the validation errors above before committing."
  exit 1
fi
